namespace BinkyRailways.Core.Model
{
    /// <summary>
    /// Passive junction reference with intended state (which is always non-existing).
    /// </summary>
    public interface IPassiveJunctionWithState : IJunctionWithState
    {
    }
}
